pls extract fils from "models.zip" to folder "models"
If not done a new model will be trained when programm is started, wich will take ~45min by 100% CPU usage X(

The Model was to big to upload to Github directly